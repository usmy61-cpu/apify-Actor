# Apify SDK
apify-client>=1.6.0
apify>=2.0.0

# Scraping — tiered approach
python-jobspy>=1.1.82      # LinkedIn + Indeed (handles TLS fingerprinting) — pip install python-jobspy
playwright>=1.44.0          # JS-heavy sites: Jobs.ch, Indeed fallback
beautifulsoup4>=4.12.0      # HTML parsing for easy sites
requests>=2.31.0            # HTTP for easy sites (Jobscout24, Topjobs, Alpha.ch)
lxml>=5.2.0                 # Fast HTML/XML parser for BS4
cssselect>=1.2.0            # CSS selector support

# Utilities
python-dateutil>=2.9.0      # Flexible date parsing
fake-useragent>=1.5.0       # Rotate User-Agent strings
tenacity>=8.3.0             # Retry logic with backoff
httpx>=0.27.0               # Async HTTP (generic scraper)
extruct>=0.17.0             # JSON-LD / microdata extraction (structured data)
w3lib>=2.1.2                # URL utilities
